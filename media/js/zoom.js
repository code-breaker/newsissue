function zoomIn(){	var zoom = document.getElementById('ebody');	size = getCookie("size");	if (size !="" && size !="NaN")	{		var size_num = parseFloat(size, 10);			}else{		var size_num = parseFloat(zoom.style.fontSize, 10);	}	size_num = size_num+1;	document.getElementById('ebody').style.fontSize = size_num +"px"	setCookie('size',size_num);}function zoomOut(){	var zoom = document.getElementById('ebody');	size = getCookie("size");	if (size)	{		var size_num = parseFloat(size, 10);			}else{		var size_num = parseFloat(zoom.style.fontSize, 10);	}	size_num = size_num-1;	document.getElementById('ebody').style.fontSize = size_num +"px"	setCookie('size',size_num);}function setCookie(name,value){    document.cookie = name + "="+ escape (value) + ";";}function getCookie(name){    var strCookie = document.cookie;    var arrCookie = strCookie.split("; ");    for(var i=0;i<arrCookie.length;i++){        var arr = arrCookie[i].split("=");        if(arr[0]= name) return arr[1];	}    return "";}function getFontSize(){	var size = getCookie('size');	if (size !="" && size !="NaN"){        document.getElementById('ebody').style.fontSize = size+"px";	}else{		document.getElementById('ebody').style.fontSize = "12px";	}}